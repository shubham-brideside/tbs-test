{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "serve -s dist -l 8080",
    "postbuild": "copy web.config dist\\web.config"
  },
  "engines": {
    "node": "18.x"
  }
